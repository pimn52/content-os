# Content OS — 开发基线修订

日期：2026-09-07。本文在冲突处优先于 PRD.md 和 DEVELOPMENT_PLAN.md；未冲突部分保留。

## 产品方向

名称统一为 Content OS。OSS 仅是 Open Source Software 的常见缩写，不属于产品名；更名不改变既定开放接口方向，也不自动确定发布许可。

长期定位以用户修改后的《产品战略基线报告 V0.1》为准：帮助专业创作者和小团队发现值得表达的内容，以自己的方式、可控成本持续经营内容资产。Know what to create → Create it as you → Learn what works。

价值排序保持 Market Intelligence、Content Decision、IP Personalization、Automated Production、Performance Learning。工程上先验证生产链路，不表示将长期产品改成自动剪辑器。市场价值尚未验证，不能用成片能力代替市场需求验收。

## 最后修订

1. 区分阶段：M0 数据契约与运行骨架；M1 本人真实素材理解、场景匹配与基础成片；M2 Voice/Talking、账号智能、移动采集；M3 市场机会与效果学习。M0 不等于 PRD 完整 Alpha。
2. 将最小持久化任务状态、幂等标识、失败重试和成本记录前移到长耗时媒体任务之前。Gate 7 保留完整恢复与故障强化，不能到那时才引入任务基础。
3. M1 使用用户提供的稿件、原音轨或已授权旁白，先验证镜头选择；自动本人声音和新口播在 M2 验证。原口播仅在语义匹配时使用，不得让旧口型冒充新台词。
4. 手动 IP 定位与确认在 M1 前可用；自动推断、被拒内容和用户修改应记录证据来源。个性化不能只靠脸与声音。
5. Account Provider 按实际权限和字段可用性工作；不要将历史元数据等同于原视频下载权限。字幕、分析数据不可用时明确记录缺失，支持手动导入，不捏造指标或阻塞创建流程。具体接入时核对官方 API。
6. Router 按场景类型、最低质量、语义相关度和可用预算挑选候选；已有素材和历史素材统一资产模型，以使用记录降权。不得为了复用率强用不相关素材。
7. 补拍是可选方案：分别显示现金/API成本与用户预计耗时；用户拒绝补拍后继续可行替代，无法满足则明确缺口。以前的价格与匹配百分比仅为示例。
8. VideoSpec 用整数帧和明确帧率，源片段用整数毫秒。保留来源、授权、schema_version；禁止负时间、越界、NaN、隐式货币混加。
9. 预算机制需在付费调用前完成估算/预留，在完成或失败后对账；重试同样受预算约束。未知价格不能当作零元。当前开发批次不调用付费服务。
10. 本地优先：React/Vite/TypeScript → FastAPI/Pydantic → SQLite 与轻量 Worker → Provider → VideoSpec → Remotion/FFmpeg。不引入微服务、Redis或强制GPU。首批 API 仅监听回环地址；手机接入前再完成认证及访问控制。
11. Windows 路径必须允许空格和中文。Python 与 Node 分离环境，媒体存到独立数据目录；本次云工作区无法直接写入用户 Windows 路径。
12. 保留 AGPL 作为历史候选，不在本批次声明已选许可。Remotion、FFmpeg构建与模型权重许可在实际采用与发布前分别核验，不以一个项目许可证覆盖所有依赖。

## 开发调度

| 批次 | 内容 | 执行 Agent | 放行依据 |
|---|---|---|---|
| 当前 Task 001 | 核心 Pydantic 契约、示例、验证 | Terra | 有效数据往返与非法数据拒绝 |
| 当前 Task 002a | FastAPI 启动骨架、健康检查、Windows 指南 | Luna | 健康端点测试通过 |
| 当前 G0 Review | 跨模型边界审查 | Sol | 阻断问题已修复 |
| 接续 Task 002b/003 | Web/Renderer 骨架、SQLite、任务基础 | Luna；并发恢复部分 Terra | 启动与持久化集成通过 |
| Task 004–010 | 导入、探测、切片、理解与搜索 | Luna 做封装/UI；Terra 做媒体与模型逻辑 | 实际媒体及检索验证 |
| Task 011–015 | ScenePlan、Router、VideoSpec、渲染 | Terra；简单页面 Luna | 真实素材 9:16 成片、用户质量验收 |

每批只启动依赖已满足的任务。模型按能力分层控制成本，本文不承诺实时费率或最小总账单。

## 验收边界

第一真实素材 Gate 需用户的 3–5 条视频和一份脚本；记录可用片段比例、Top-1/Top-3 选择、替换次数、人工耗时、实际成本。不将合成测试素材的通过误报为真实 IP 效果通过。Voice/Talking 质量单独验收，失败时保留原片段与 B-roll 路径。
