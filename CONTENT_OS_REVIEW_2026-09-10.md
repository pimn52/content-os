# Content OS 实施复审与下一阶段执行包

审查日期：2026-09-10  
代码基线：`f4da6135ee2c1a8d75e945f9ae7721f1e15e50ad`，仓库 `pimn52/content-os`。与上一轮 `3dd3896` 比较，本次提交涉及 122 个文件、增加 16,262 行、删除 201 行，其中包含生成契约、锁文件、文档和测试，不能将增量行数等同于产品完成度。

## 1. 结论

**应保留现有底座，收紧执行主线；目前仍是有较多外围功能的内部 Alpha，尚未通过真实成片验收，更没有完成替代拍摄。**

不是现有声音克隆或对口型算法效果差：这两条生成能力在当前代码中还没有实现。`runtime.py:203–204` 明确将 TTS 和 Talking 标为 `not_developed`；Profile 接口主要登记授权、参考片段和外部 profile ID，不执行声音克隆、语音合成或对口型。

已有自动剪辑结果不能代表最终目标失败，但继续打磨旧片硬切、手工逐场景录音，不会自然变成新口播生成。应先验证真正困难的能力，再完成产品集成。

长期定位仍是 **Know what to create → Create it as you → Learn what works**，包含内容决策、IP 知识/观点/表达、生产和效果学习。本轮优先攻克“Create it as you”中的生产瓶颈，不把整个产品改成只做数字人。

## 2. 证据范围

- 已读取最新代码、测试、状态和决策文档，并运行独立验证，未修改产品源代码或推送提交。
- 当前环境不能直接读取 `codex://threads/01a08519-a406-7462-83c8-2557fc2a481e`；上下文检索也未取得这条最新会话。因此不判断实施模型当时是否忽略了某条口头授权，不将仓库记录当成完整会话。
- 未取得本地被忽略的真实素材、用户生成 MP4、GPU 情况和当前密钥配置；不能独立评价本人相似度、口型自然程度或用户电脑速度。
- 仓库 `STATUS.md:123–130` 记录了两条旧素材草稿及 U1 失败反馈，承认没有新旁白/新口型。这与用户本轮反馈一致。
- 测试用假 transport 只用于复现调用边界，未调用外部收费模型。复现脚本与报告一同提供。

## 3. 已有工作的实际价值

| 能力 | 当前状态 | 不应被误解为 |
|---|---|---|
| SQLite、契约、作业、草稿与恢复 | 已有可复用实现 | 成熟产品已交付 |
| 素材导入、切片、音频/图片导入、分析结果写回 | 已有实现 | 所有真实素材已经准确理解 |
| ScenePlanner | 有模型适配器，并接入 IP/素材上下文 | 已证明能稳定写出符合本人观点的新文案 |
| ASR | 云兼容接口及新增可选 faster-whisper 本地适配器 | 用户电脑已安装模型并完成真实语音质量验证 |
| 素材路由 | 检索、真实素材优先、复用降权、补拍/静态兜底 | 完整的声音/口型/AI 视频混合路由 |
| 配音与渲染 | 能导入已有授权音频并组装 | 自己生成本人的新声音 |
| Voice/Talking Profile | 授权与资料登记 | 声音克隆、对口型或数字人生成 |
| 账号、机会、发布与反馈 | 多为导入/登记与记录流程 | 自动同步、市场研究、持续学习闭环 |
| 移动入口、运行状态与启动脚本 | 有基础实现 | 手机真实设备及远程运营已验收 |

真实分析与模拟测试分开、未知成本不冒充免费、原声未改时不伪装成新稿，这些都是正确修正，应保留。

## 4. 需要修正的代码问题

### P1-A：重试幂等只保护账本，未保护外部执行（已复现）

位置：`services/api/app/budget.py:120–145`；`main.py:541–574,1882–1919`。

`reserve()` 在遇到已有幂等键时直接返回旧记录；调用方随后仍执行 `planner.plan()`，不检查旧任务是否已完成，也不返回原结果。请求身份只包含项目/操作等，未绑定实际 script/topic/context 的内容摘要。

复现：同一项目、同一个 `Idempotency-Key`，先后发送不同 topic，两个响应都是 200，transport 执行 2 次，账本只有 1 条。这会造成重复消费和成本低报。

修正：以操作、输入摘要、IP/脚本版本、provider/model 等定义请求身份；同键异输入返回 409；同键同输入返回持久化结果或已有 job 状态。执行权必须只有一个持有者。不能仅让 reserve 返回同一记录。

验收：相同请求重试实际调用一次；并发重试只执行一次；中途崩溃恢复不盲目重复收费；异输入同键拒绝。

### P1-B：全局预算并未跨项目执行（已复现）

位置：`budget.py:149–163`；`main.py:516–527`。

虽然读取 global policy，但检查的是 `list_for_project(project_id)`。复现：全局 `max_calls=1`，两个项目分别发起一次规划，都返回 200；全局统计随后显示 2 次且 `over_budget=true`。

修正：明确“全局总预算”和“项目预算”同时约束；如需要“项目默认策略”，另设字段，不与总上限混用。预算读取、检查、预留需在同一原子边界内完成。当前检查发生在写事务之前，另存在并发超额窗口（静态发现，本次未做并发压力复现）。预算周期也应明确定义，不能将累计账本直接标为月预算。

验收：两个项目共同消耗全局额度；项目额度不能覆盖全局硬上限；并发超额请求在真正执行前被拒绝。

### P1-C：部分外部调用绕过账本或计数不准确（静态确认）

位置：`main.py:1842–1860,1936–1958`；`routing/asset_router.py:129,148–158`；`search/pipeline.py:49–64`。

`/clips/search` 在 embedding 模式直接执行 query embedding，没有预留预算。多 Scene 的路由只预留一条记录，但 `route_all()` 逐场景检索，每次 search 都调用 embedding provider；按 HTTP 请求数记录不能等于实际 provider 调用数。

修正：由统一应用服务封装每次真正的 provider 操作、预算、结果、usage 和重试；可以批量 embedding 减少调用，但必须按真实批次与使用量记账。不能依赖某个具体 provider 的 `isinstance` 判断来决定是否受预算约束。

### P1-D：配音较长时，组装器产生越界镜头（已复现）

位置：`assembly/video_spec.py:190–214`；`renderer/remotion.py:181–197`。

新稿模式将 clip_end 改为 clip_start + 音频时长，然后拿这个已改写区间检查时长，掩盖了原始 Clip 长度不足。复现：合法片段 0–1500ms，音频 2000ms，组装仍成功并输出 0–2000ms。渲染器稍后会按存储的 Clip 边界拒绝，因此问题推迟到了渲染阶段。

修正：始终使用原始 Clip 边界；语音长于画面时由 Timeline Planner 自动选择多镜头、允许的静态延展或新口播，不越界、不无声截断语音。若当前能力无法补足，在组装前给出具体缺口。

验收：短片段+长语音、非零片段起点、不同帧率、同一语音跨多镜头都有集成验证。

### P2-A：强制逐场景独立音频，把内部限制转成用户劳动

位置：`assembly/video_spec.py:118–142` 及对应测试。

目前同一 AudioAsset 绑定多个新稿 Scene 会被拒绝，报错要求用户先把整段录音拆成逐场景文件。此限制有防止音频重复从头播放的动机，但产品需要解决的是时间轴引用。

修正：支持共享主语音轨上的时间区间；用时间戳/对齐结果映射句子、字幕和画面。生成端可按自然段合成以兼顾重试和韵律，再自动合并/对齐；不要求用户手工拆文件。旧音频导入保留为可选输入，不再作为替代拍摄验收条件。

### P2-B：运行就绪与端到端测试命名容易夸大覆盖范围

`runtime.py` 的 provider ready 表示配置了 key，并未探测；render ready 主要检查 npm。应区分 implemented / configured / locally_available / verified，避免 UI 将“配置存在”显示成“已能生成”。

`test_full_local_e2e_import_analyze_plan_match_voice_gate_talking_and_render` 虽含 voice/talking 名字，末尾实际断言 TTS/Talking **未开发**。它验证的是本地流程与未开发状态，不是新口播端到端。保留测试，改准确名称，新增真正的生成验收。

### P2-C：集成层开始集中堆积，应局部整理

`main.py` 已达 2212 行，同时处理项目、素材、预算、账号、规划、渲染等。同步 provider 调用仍在 async handler 内执行，单进程会阻塞其他请求。新语音/口型生成应进入已有作业体系；按 provider 调用服务、创作编排服务拆分必要部分。保持当前 API/DB/worker 架构，不重写全仓库，不引入微服务、Redis 或复杂工作流框架。

## 5. 为什么出现目前的偏差

这里有三层问题，不能归咎为“Luna 不行”。

1. **交付定义不够聚焦。** 我此前给出的执行规格允许导入配音+B-roll 作为早期闭环，Talking 后置；这适合降低基础链路风险，但不能满足你现在明确的替代拍摄目标。应修正我给出的推进顺序与验收口径，而非继续要求你接受降级成片。
2. **后续阶段横向铺开太早。** S2/U1 未过，S3/S4/S5 已做了很多表单、状态和记录。代码可复用，但这些不消除声音/口型核心的不确定性。
3. **代码测试覆盖与产品质量证据混淆。** 很多单元测试证明契约、状态与报错；少量真实色块/音频渲染证明文件链路。都不能证明观点像本人、声音像本人或口型自然。

阶段判断：S0/S1 有明显实质进展；S2 核心成片与 U1 尚未验收；S3–S5 有部分实现，不能据此宣布整体阶段完成。不提供缺少依据的“完成百分比”。

## 6. 冻结的下一阶段目标

> 一个已授权的创作者提供一次旧素材/清晰样本并确认 IP，之后输入一个新主题，系统生成可修改的新文案，合成本人的新声音，生成至少一个说出新台词的本人对口型镜头，组合自己的 B-roll/排版、字幕与封面，导出 30–60 秒新视频。用户不为每条视频重新拍摄、录音、剪音频或手工切片。

这是本轮“替代拍摄”验收目标，不代表 Market/Performance 长期目标取消。旧样本不足时可一次性引导补录/补拍；不承诺任意坏素材都能修复。未达到质量门槛不得自动宣布完成，也不能静默改用陌生 TTS、旧原声或通用数字人。

## 7. 按依赖连续执行的工作包

| 顺序 | 工作包 | 可审查产物与验收 | 最低适配分工 |
|---|---|---|---|
| A | 合并当前约束，修 P1-A/B/C/D | 原位更新既有执行规格/AGENTS/STATUS；复现转回归；不创建新的平行最高规格 | Terra 负责跨模块修正；Luna 跟进独立文档与 UI |
| B | 真实能力小样 | 本地环境检查；一个授权样本、两段未录过的新台词；真实 clone/TTS 音频与 5–10 秒新口型视频；保留输入输出、provider/model、耗时成本与失败结果 | Terra 实现；Sol 在路线失败或关键质量判断时介入一次 |
| C | 新文案与 IP 上下文 | 真模型输入知识/观点/风格及引用来源；持久化 script revision；用户更改后后续结果失效；同主题不同 IP 的对比证据 | Terra |
| D | 语音为基准的时间轴 | 主语音轨、句段时间、字幕、镜头区间；多镜头覆盖长语音；新增生成资源及来源记录 | Terra；Luna 做界面及进度 |
| E | 真实生成编排 | Script → Voice → Talking → Router → Render；后台可恢复任务、缓存、预算、错误分支；脚本变化只重做必要步骤 | Terra |
| F | 创作者闭环验收 | 两个不同新主题各产出一条 30–60 秒视频；至少一段真实新口型；在正常 UI 完成，不通过开发者手改 DB/JSON 才能成功 | Terra 集成，用户仅审最终结果 |
| G | 产品化及运营闭环 | 在 F 通过后接续账号/机会/反馈、移动设备、安装升级备份和稳定性任务 | 依既有计划，Luna 承担隔离明确的小任务 |

能力小样 B 是一项有上限的技术验证：先检查本机 CPU/GPU/显存、可用授权样本、已获准 provider 和费用限制；从可行路径中选主路线，必要时最多比较一条备选。无需先把全部旧视频转写、全部账号导入才开始。不要接入五个 provider 后仍交不出一段新口型。

本报告不指定未经硬件、官方能力/许可和真实样本验证的具体模型。选择候选时查官方资料并记录版本、调用成本、授权方式与硬件条件。Local-first 不等于所有计算必须本机完成；云路径须在既有允许的服务、数据和预算范围内运行。若没有这些授权，把具体样本清单、服务和限额集中呈现一次，其他本地任务继续。不可绕过既有自动审批拒绝或把旧 key 当作所有内容外发的无限授权。

## 8. 关键节点和验收标准

### U-Voice：一次能力确认

提供原样本、两段新台词、对应新音频和新口型片段。用户主要确认“是否像我、是否愿意采用”。实现方先完成可播放性、台词完整性、音画长度、重复/漏句、明显音画偏移的检查。人声相似度和自然度不能仅凭 ASR 分数通过。

若两条允许路径均失败，提交具体对比和瓶颈，触发一次目标/资源决策；不能无限微调剪辑，也不能偷偷取消新口型。

### U-Product：最终阶段验收

- 两个新主题，脚本忠于已确认的知识/观点；模型不把未知内容伪装成用户主张。
- 新台词确实由本人授权声音合成；至少一个本人镜头说的是新台词，非旧台词回放。
- 30–60 秒，台词完整、无明显句尾截断、无重复从头播放、字幕对齐、主体构图可接受；本人镜头无明显口部破损或身份跳变。
- 用户无需逐场景准备录音。自动生成流程从 UI 发起，可看进度、失败原因和重试结果。
- 修改一句话后旧结果不能被当成新结果；任务重试/恢复不会无记录重复消费。
- 实际成本可追溯，未知成本单列；预算在执行前生效。记录人工操作时长，10–20 分钟是待实测的产品目标，不作为已达成事实。
- 无 Codex 会话参与也可正常运行；所需运行时依赖和 provider 配置有检查与安装说明。

这两个节点之外不逐任务问“是否继续”。内部测试通过即执行下个 ready 任务；普通重构、错误修复、UI 完善由实现方负责。只有新增付费/外发范围、破坏性迁移、核心目标改变、现有路线证实不可行才集中请求决策。没有条件做模型真实验证时，明确阻塞项并继续不依赖它的任务，不能用模拟输出验收真实能力。

## 9. 给本地 Codex 的执行指令

> 阅读此报告并核对当前 HEAD；若 HEAD 已变化先审查相关差异。保持 Content OS 的 Local-first、BYOK、个人 IP 内容运营定位。保留现有可用底座，原位更新当前执行规格和 AGENTS，明确新文案→本人新声音→本人新口型→成片为下一阶段核心验收，停止将导入配音或旧片剪辑作为该目标完成依据。先修报告 P1，再按 B–F 依赖持续实施，暂停非必要外围模块。Terra 负责媒体/生成/时序/预算跨模块，Luna 负责已定契约下的隔离 UI、CRUD、文档和明确回归；遇到核心能力瓶颈再升级审查，不以最低单价替代交付责任。完成一个工作包自动接续下一个，遵守既有外发/费用授权，只在 U-Voice、U-Product 和真实授权/资源阻塞时集中返回。每个包提交包含真实产物与验收证据，STATUS 顶部只维护当前事实，历史日志分离。不重写产品、不反复重定义边界、不把未实现能力包装成就绪。

## 10. 独立验证记录

验证结果见本报告末尾补充。复现脚本 `reproduce.py` 使用临时 SQLite 和本地假 transport，不调用收费 API；音频占位字节仅用于验证组装契约，不用于宣称音频或生成质量。

- 在独立 Python 3.12 虚拟环境安装项目声明的 test 依赖后，运行 `python -m pytest -ra --tb=short`：**319 passed，4 failed，1 skipped，2 warnings**（324 项）。
- 其中 1 项失败为 `test_local_executable_resolution_prefers_explicit_override_and_bundled_tools`：在 Linux 环境却直接断言存在 Windows node_modules 的 ffprobe 路径，未通过 fixture 隔离平台/文件存在条件。这是测试可移植性问题，不证明用户 Windows 上 ffprobe 失效。建议模拟文件存在/平台并按支持平台分别验证。
- 另 3 项为真实 Remotion 渲染集成测试。本环境首轮缺少 renderer 依赖；补装后检查能完成代码 bundling，但 Chromium Headless Shell 下载发生代理连接超时，网络授权流程取消，因此本次无法完成真实渲染复验。不能把它们直接归为产品渲染逻辑缺陷，也不能声称本次全绿或真实成片已验证。需要在用户已配置的 Windows 环境复跑这 3 项并保留输出。
- `apps/web` 执行 `npm ci --ignore-scripts --no-audit --no-fund` 与 `npm run build` 成功（TypeScript + Vite）。构建成功不等于浏览器操作或视觉质量验收。
- `reproduce.py` 独立输出：相同 key 两次执行、账本仅一条；全局上限 1 时第二项目仍可调用且总账显示 over_budget；组装器把 1500ms Clip 扩成 2000ms。以上三个问题不依赖 Chromium 或外部服务。
- 首次直接用 `pytest` 命令遇到 `scripts` 导入路径收集错误；改用仓库根目录 `python -m pytest` 后完成上述测试。建议将标准命令固化到 CI/开发说明。
- 未做真实声音克隆/对口型测试，因为当前对应能力未实现；未将 mock planner、测试波形或登记 Profile 计为能力验证。
- `git status --short` 在验证后无源代码改动。当前 Python 依赖使用宽版本范围，未见 Python 锁文件；为跨机器复现建议记录已验收依赖与 Python 版本。此项无需引发框架升级或重构。

复现运行方式：先在仓库虚拟环境安装 `.[test]`；在仓库根目录执行 `python <报告附件路径>/reproduce.py .`。输出用于审查失败行为，脚本不是修复补丁。将本报告交给本地 Codex 即可按第 9 节继续，无需复制本审查环境或替换整个仓库。
