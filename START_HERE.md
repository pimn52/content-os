# 本地 Codex 接续入口

把本包三个 Markdown 文件放进 Content OS 项目根目录。不要覆盖其他代码，不要 reset 当前分支。

向本地 Codex 下达：

请以 CONTENT_OS_EXECUTION_SPEC.md 为 R1 的统一执行规格，AUDIT_REPORT.md 为审查起点证据。先对比当前 HEAD 和未提交改动，按规格第 0 节同步旧文档入口，保留现有成果。先修 S0 的实际交互阻断并做浏览器流程验证，再按 S1–S5 连续推进。外部付费接口保持后置，优先真实模型辅助测试，不用固定夹具替代语义结果。普通工程选择不再反复确认；只在 U1/U2/U3 产品节点或明确预算、权限、不可逆/目标变更时集中返回。更新 STATUS.md 后直接领取下一个 ready 任务，不停留在推荐下一步。遇到用量限制保存准确接续状态，不重启战略讨论。

本文仅是入口；所有边界、验收和模型分工以统一执行规格为准。若本地有审查提交之后的修复，验证后标记完成，不重复修改。
