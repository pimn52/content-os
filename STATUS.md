# Content OS — Current Status

Updated: 2026-09-14 (Asia/Shanghai)

## Current truth

- Latest repository work has completed the main local media/runtime foundation: persistent IP/draft revisions, provider accounting/idempotency, local media import and continuous clips, ASR/vision/index boundaries, ScenePlan/Router, cost controls, browser/mobile upload, `MasterNarration` timeline work, Remotion rendering, Jobs/recovery and backup/release checks.
- The project is still an **internal Alpha**. It has not yet passed the product gate “new creator voice + new creator Talking/lip-sync + 30–60s new video”.
- **Superseding provider decision (2026-09-14):** ordinary, consented day-to-day creator footage is the required Talking input. A special silent/closed-mouth AI-only recording is not an acceptable product prerequisite or admission test. Chatterbox is rejected for voice quality and MuseTalk 1.5 is rejected for visible sync/performance quality; their isolated local runtimes, weights, caches, generated outputs and MuseTalk helper scripts have been removed. Historical notes below explain the evidence but do not make either provider available or acceptable.
- VideoReTalking evaluation has started in its own local data directory. A clean Python 3.8 runtime was created after pinning `setuptools<75` to avoid a reproducible Conda-forge cache defect; its official CUDA 11.1 PyTorch 1.9.0 wheel (3.1 GB) is downloading in a hidden background process. No VideoReTalking weight, generated asset, runtime-quality, license or product-admission claim exists yet.
- Imported narration and source-led real-media renders are engineering/fallback paths, not proof that repeated filming has been replaced.
- TTS/voice cloning and Talking/lip-sync remain the next core capability area. Do not resume broad peripheral feature work before this uncertainty is tested.
- The local development machine previously probed as an RTX 3060 Laptop GPU with 6 GiB VRAM. This can justify bounded local experiments, but Content OS must retain a non-high-end-GPU fallback path.
- `MasterNarration` Web build and isolated browser regression are now verified: ScenePlan → candidate persistence → timed SRT binding → aligned master-track selection → three-scene VideoSpec completed through the UI. The regression fixed two actual S0 blockers: asynchronous forms retain their form element before reset, and route persistence strips UI-only `shoot_list` before submitting the strict `DraftRoute` contract. This is UI/timeline evidence only, not Voice/Talking quality evidence.
- Voice now has a provider-neutral persisted request/job boundary: a consented `VoiceProfile` plus credential-free copy/authorization input becomes `GENERATE_VOICE`; the handler reserves/reconciles `tts`, imports the local output as an `AudioAsset`, and persists provider/profile/job provenance. Generated narration is blocked from VideoSpec assembly until recorded QA is `verified`. The optional OmniVoice adapter is lazy and explicitly non-commercial-benchmark-only; OmniVoice and Chatterbox remain isolated evaluation environments, with no paid-provider call and no vendor state in Core.
- Voice QA now consumes a real generated-file transcription/alignment result and persists provider/model evidence, copy coverage, missing/duplicate tokens, leading/timed-segment gaps, long silence and basic local-file playability. Missing or fabricated semantic evidence is failed rather than treated as a fixture pass. Real inference evidence now exists for the corrected OmniVoice samples and the Chatterbox comparison. U-Voice judged the corrected OmniVoice pair as having no unrelated sound; the second sample is slightly short on naturalness/rhythm, but the overall result is basically acceptable. In the side-by-side comparison, Chatterbox's timbre similarity and naturalness were clearly worse than OmniVoice. This is not yet a commercial/product-quality gate.
- Gate C now has a provider-neutral `TalkingHeadProvider`, a typed `GENERATE_TALKING` payload/API and a durable local-worker handler. It accepts only an explicitly consented `TalkingProfile` and a generated narration whose Voice QA state is `verified`; it reserves/reconciles the existing `talking` provider call before generation, imports only a returned local video as `AI_VIDEO`, and persists provider/profile/narration/job provenance as `talking_generation.qa_state=pending`. It rejects unverified narration before a provider is called, so an imported recording or source-led recut cannot silently satisfy the new-Talking gate. This boundary is unit/API-verified and one real local provider output now exists.
- MuseTalk 1.5 was evaluated only as an optional local Talking candidate. Its Python 3.10 runtime, CUDA 11.8 Torch 2.0.1 / TorchVision 0.15.2 / TorchAudio 2.0.2, official MMLab preprocessing stack and model weights are isolated under `content-os-data/musetalk*`; the RTX 3060 Laptop GPU is visible to that runtime. No HF/Kimi/API secret and no paid inference were used. The initial Hugging Face CLI transfer could not fetch large-file metadata through the mirror, so the public checkpoints were fetched directly with resume support. `scripts/generate_musetalk_output.py` keeps all provider/cache/temp state outside Core, compensates for upstream Windows unquoted-path and console-encoding bugs, and uses the bundled Remotion FFmpeg. Its batch-size-one run produced `content-os-data/musetalk-evaluation/outputs/biyingjie-zh-new-lipsync-v15.mp4` from the consented Biyingjie Chinese face reference and new 3,960ms generated narration. On this 6 GiB GPU the real run completed at batch size one; the earlier batch-size-two attempt was blocked before inference by a missing S3FD cache path, so it is not recorded as an OOM result.
- The generated MuseTalk MP4 is H.264/AAC, 1280x720, 3,960ms and 1,065,184 bytes. `scripts/evaluate_talking_output.py` independently extracted its actual audio and ran local faster-whisper evidence: target copy coverage 100%, zero missing/duplicate tokens, 0ms leading gap, 680ms longest gap, 3,968ms observed output audio and a 3,960ms video container. Automated playability/new-word/duration checks therefore passed. This does **not** automatically prove visible lip-sync, creator likeness or naturalness. The source clip's old burned-in subtitles conflict with the new words, so this exact file is a technical Talking evaluation, not a final production scene.
- U-Talking judged that exact MuseTalk sample “basically acceptable” as a technical candidate. This passes the narrow human technical-candidate gate for likeness/naturalness/lip-sync, but does not promote its landscape, baked-subtitle reference into a final production scene. MuseTalk retains source gaze, head movement and most expression; it cannot turn a source ending away from camera into a direct-to-camera ending. A future final scene must therefore use a reference clip selected or recorded for the requested start/end gaze and expression, then use a separately selected generated voice take for delivery.
- Talking reference selection is now an explicit product boundary rather than an informal review note. A `TalkingPerformanceBrief` specifies required start/end gaze, expression labels, minimum reference length and whether burned-in subtitles are forbidden; each reference Clip can persist sourced observed evidence for those facts. The selection API returns either the highest-scoring evidenced Clip or a concise capture instruction—unknown evidence never silently passes. A `GENERATE_TALKING` request now names one Clip from the consented `TalkingProfile`; the worker resolves its exact local path and time interval, passes that `TalkingReference` to the provider, and records the selected Clip in output provenance. This is contract/API evidence only: the assessment endpoint accepts human or assisted evidence, and an automatic multi-frame assessor has not yet been run or claimed.
- The optional `MuseTalkLocalRunner` now invokes the isolated bridge with the selected source path plus exact start/end milliseconds; it remains unconfigured by default and downloads no weights or credentials. The bridge extracted and generated from Biyingjie's authorized Chinese `30,000–35,000ms` interval, producing `biyingjie-zh-new-lipsync-v15-selected-interval.mp4` (H.264/AAC, 1280×720, 3,960ms, 1,032,197 bytes). Independent output-audio ASR again verified the new copy at 100% coverage with zero missing/duplicate tokens, 0ms leading gap and a 680ms longest gap; output audio was 3,968ms. This proves selected-interval execution, not visual quality. That particular source interval still contains burned-in original subtitles, so it remains evaluation-only and cannot satisfy a no-subtitle final-scene brief.
- The local reference bridge now supports reversible simple preprocessing before inference. A Clip whose sourced assessment confirms burned-in subtitles passes a `0.18` bottom safety-crop ratio through the normal selected-reference provenance; the bridge extracts that interval and crops before MuseTalk, never altering source originals. A real crop run produced `biyingjie-zh-new-lipsync-v15-selected-interval-subtitle-crop.mp4` (H.264/AAC, 1280×590, 3,960ms, 820,861 bytes); independent ASR/audio-duration QA again passed at 100% new-copy coverage, zero missing/duplicates, 0ms leading gap and 680ms longest gap. Frame review confirms the prior bottom subtitles are removed without cutting the face/mouth. It also confirms the limit of simple processing: the result remains landscape and ends only near—not stably at—camera gaze. It is technical evidence only, not a final vertical Talking scene or a substitute for performance selection.
- `scripts/sample_talking_reference_frames.py` now records five reproducible timestamped frames and a JSON evidence manifest per authorized source without inventing visual semantics; all four latest authorized Downloads MP4s have current frame evidence. The local runtime has no installed OCR, MediaPipe or dedicated gaze/expression model, so the manifests correctly retain `frames_only` semantic status rather than fabricating a model result. Assisted visual review found a stronger Biyingjie Chinese candidate at `819,000–824,500ms`: it reaches a near-direct, natural ending but contains prior subtitles during the spoken interval. The normal interval bridge plus the reversible `0.18` subtitle crop produced `biyingjie-zh-direct-ending-subtitle-crop-lipsync-v15.mp4` (H.264/AAC, 1280×590, 3,960ms, 1,046,693 bytes). Independent QA again verified 100% new-copy coverage, zero missing/duplicates, 0ms leading gap and a 680ms longest gap; reviewed 1.5s/3.3s output frames show no old subtitle, an intact face/mouth and a substantially more direct final gaze. This is the strongest technical Talking candidate so far, but still needs the normal vertical assembly and U-Talking product-quality judgment.
- The normal local worker is now wired for optional MuseTalk execution: explicit `--ffprobe` resolution accompanies `--ffmpeg`, so imported Talking outputs do not rely on a system PATH binary. `scripts/run_talking_worker_evaluation.py` performs a non-fixture, isolated application-path run: it imports the authorized reference and an already QA-verified generated narration, records the selected `819,000–824,500ms` Clip with sourced human evidence, enables the reversible `0.18` subtitle crop, and invokes the ordinary `generate_talking` worker. The successful run at `content-os-data/talking-worker-evaluation-20260913-v3/` completed its job and zero-cost local provider ledger entry, imported exactly one H.264/AAC `AI_VIDEO` asset (1280×590, 3,960ms, 1,044,252 bytes), and persisted its Clip, narration, provider and crop provenance with `qa_state=pending`. It used no download, API credential or paid inference. This proves durable worker composition and preprocessing provenance; it does not replace the separate visual/sync QA or final vertical product gate.
- The independent Talking evaluator can now promote only a real generated `AI_VIDEO` asset after its own FFprobe/extracted-audio/local-ASR evidence succeeds; the worker output passed at 100% new-copy coverage with zero missing/duplicate tokens, 0ms leading gap and a 680ms longest gap, and is stored as automated-QA-verified while retaining the explicit U-Talking human gate. The assembler and renderer reject pending/failed AI Talking assets, so a provider output cannot bypass that boundary. The normal render worker now composes its audio/image repositories just like the API path. It also supports explicit, evidence-backed vertical treatment: `contain` remains the default; `center_crop` and a bounded source-bottom safety crop require a review reference and are render-only, never modify originals or claim automatic face detection.
- A real normal-worker Gate D integration render now exists at `content-os-data/talking-worker-evaluation-20260913-v3/renders/ac3d0499-5545-4f6f-a496-f4f8d0de8b31/f8c5dba5-82e3-468a-acf7-7ad70c559e56.mp4`: five persisted scenes, H.264/AAC, 1080×1920, 29.97fps, 1,012 decoded video frames, 33.813s and 36,143,832 bytes. It uses the verified new Talking clip for the 3.937s hook, then authorized Biyingjie B-roll and local typography. Render-time review selected center crop for the Talking hook and `0.18` bottom safety crop for B-roll whose sourced evidence confirmed old burned-in subtitles; sampled output frames confirm the original subtitles are absent while the face remains visible. This is genuine provider → QA → AI asset → normal worker → vertical render evidence, but only the initial 3.937s carries new generated narration. The silent B-roll/typography captions are intentionally documented as technical assembly, **not** a false 30–60s cloned-voice/R1 product-gate claim.
- The first Biyingjie English reference candidate was rejected for Talking evaluation because its sampled frame is side-on and its original subtitles would conflict with new speech. The Biyingjie Chinese source has a front-facing five-second reference around 00:00:30 and is selected for the local provider spike; its baked original subtitles mean it is **evaluation evidence only**, not a final production scene. `scripts/evaluate_talking_output.py` is now prepared to verify a real generated MP4 by FFprobe, extracted output audio and independent local ASR coverage of the new words, plus driving-audio/video duration alignment. It explicitly leaves visible lip-sync, likeness and naturalness for U-Talking rather than inventing a fixture-based score.
- U-Voice authorization covers all four latest MP4 files in `C:\Users\ASUS\Downloads` and local model downloads. Their 10-second 24 kHz mono reference candidates are isolated under `content-os-data/omnivoice-evaluation/references`; originals remain unchanged. The isolated CPU OmniVoice 0.2.1 environment is installed because the CUDA 12.6 wheel transfer stalled. The authorized resume completed via `scripts/omnivoice_download.py`: snapshot `c5fdb5ccb189668d56333f77ba2629f4cd7535f4`, 13 files, including `model.safetensors` (2,450,344,112 bytes) and `audio_tokenizer/model.safetensors` (805,665,628 bytes). The first 16-step samples are rejected as product evidence: reference 2 had 1,219ms of unrecognized leading content, and reference 4's target copied source phrases. The corrected benchmark uses sentence-bounded references, novel copy and 32-step generation: corrected reference 2 was measured-trimmed and passed strict local ASR QA (3,960ms, 100% copy coverage, 0ms leading gap, 680ms longest gap); corrected reference 4 passed without trim (3,400ms, 100% coverage, 0ms leading gap, 260ms longest gap). A fresh retry of the corrected reference 2 ran for about 10m25s on CPU but re-spoke the reference sentence before the target (7,080ms; 100% target coverage; 18 duplicate tokens; 519ms leading non-target speech), so QA rejected it. A fresh 32-step long-copy run on corrected reference 4 ran for about 12m39s and likewise failed semantic QA despite 100% target coverage (9,610ms; five duplicate tokens from an extra opening phrase; 0ms leading gap; 250ms longest gap). These failures are retained as retry evidence, not edge-trimmed into a pass. The CPU path is therefore an offline benchmark only, not an interactive R1 runtime claim.
- A new, intentionally short OmniVoice scene take was generated from the existing local snapshot without network, credentials or paid inference: `content-os-data/omnivoice-evaluation/outputs/gate-d-take-02.wav` says “第一，先写出一句能被记住的结论。” and is 3,480ms / 24kHz mono. The first independent ASR report is retained as `gate-d-take-02.qa.json`: it observed the complete wording in Traditional glyphs but the former character-level QA falsely reported four missing/duplicate Simplified Chinese tokens. Voice QA now uses the lightweight Apache-licensed `opencc-python-reimplemented` normalizer solely for Simplified/Traditional comparison tokens; original ASR wording is still retained verbatim as evidence. Regression tests prove this accepts equivalent Chinese glyphs but still rejects newly added words. The separate normalized recheck at `gate-d-take-02.normalized-qa.json` verifies 100% copy coverage, zero missing/duplicate tokens, 479ms leading/longest silence and playable output. This is only the first QA-verified scene take, not a complete master narration or R1 product result.
- The provider-neutral `VoiceTakeComposer` now accepts only locally present, independently QA-verified generated takes with actual timed transcripts, then invokes local FFmpeg to produce a 24kHz PCM WAV and offsets source ASR intervals into a provisional master timeline. Its test coverage rejects pending/unmapped/missing take media and verifies deterministic ordered timestamp offsets. A second distinct OmniVoice take (`gate-d-take-03.wav`) was generated locally in about eight minutes and initially surfaced the valid spoken glyph variant `接著` versus requested `接着`; the comparison layer now treats that Mandarin speech variant as equivalent while retaining verbatim ASR evidence, and regression coverage still rejects added words. Its normalized recheck passed at 3,960ms, 100% coverage, zero missing/duplicate tokens, 0ms leading silence and 460ms longest silence. The two verified takes were really composed (not duplicated fixture audio) into `gate-d-master-01.wav`; a fresh ASR pass over the combined 7,440ms WAV verified both sentences at 100% coverage, zero missing/duplicate tokens, 439ms leading silence, 559ms longest silence and two observed segments. `scripts/import_verified_master_narration.py` then imported it through the ordinary `AudioImporter` into the real isolated v3 evaluation database (`talking-worker-evaluation.sqlite3`) as asset `e0f35cab-3876-4400-91d4-0a88b106e3dd`, preserving independent ASR timing, QA metadata and the source-take composition manifest. A mistaken first invocation created a separate empty `talking-worker.sqlite` in the same isolated folder and imported an independent duplicate there; it contains no pre-existing project data and is intentionally retained rather than silently deleted. This is an actual QA-verified multi-take master narration, but is only 7.44 seconds and therefore not the 30–60s product result.
- A normal-worker master-narration integration render now exists at `content-os-data/talking-worker-evaluation-20260913-v3/renders/ac3d0499-5545-4f6f-a496-f4f8d0de8b31/7714908f-8532-4b4a-bdf1-7199703a24d2.mp4`: H.264/AAC, two persisted scenes, 7,440ms master audio and 8,758,548 bytes. The first failed run exposed and fixed a real renderer S0 issue: it compared a source video’s native frame count directly to the composition frame count, so an authorized 25fps Talking interval was falsely rejected in a 29.97fps project. Renderer validation now checks the last composition frame against the authorized Clip duration in milliseconds, which permits rate conversion without permitting playback beyond the Clip. A focused regression test covers that 25fps-to-29.97fps path, and the corrected normal worker completed. This is multi-scene master-audio/timing proof only, not a 30–60s full-new-voice result.
- The verified OmniVoice scene takes were genuinely composed into `gate-d-master-05.wav` (30,160ms, 24kHz mono) and imported through the normal `AudioImporter` as asset `a23a0603-d352-4763-b0d1-1452d226a402` in the isolated v3 evaluation DB. Its independent ASR QA verifies all seven intended sentences at 100% coverage, with zero missing/duplicate tokens, 400ms leading silence and 900ms longest silence. The normal Render worker then completed a five-scene vertical full-flow proof at `content-os-data/talking-worker-evaluation-20260913-v3/renders/ac3d0499-5545-4f6f-a496-f4f8d0de8b31/716c4454-1b10-4534-9ffa-c722bd9481b9.mp4` (H.264/AAC, 1080×1920, 29.97fps, 30,208ms, 38,061,807 bytes): the first 3,480ms is the independently QA-verified new Talking segment and the remaining four timed scenes are authorized continuous B-roll with evidence-backed center/bottom crops and master-track captions. A separate evaluation of the rendered MP4's extracted audio (`full-master-narration-render.qa.json`) independently verified 100% full-copy coverage, zero missing/duplicate tokens, 499ms leading silence, 940ms longest silence and a 48ms audio/video duration delta. This is real local provider → QA → durable audio/AI asset → normal worker → 30s vertical render evidence. It does not replace U-Talking visible-sync/likeness/naturalness or U-Product editorial-quality judgment; OmniVoice weights remain non-commercial benchmark-only.
- **U-Talking/U-Product failed for that full-flow proof.** Human review reports the visible lip movement is wholly mismatched, the person’s naturalness and ending expression do not match the delivery, and the otherwise similar voice is perceptibly a sequence of short stitched takes rather than one 30s spoken performance. Therefore the MP4 is demoted to technical pipeline evidence only and must not be presented as a candidate delivery. The failure creates two bounded repairs: inspect/verify Talking’s actual driving-audio-to-frame alignment before another provider run, and use a verified single continuous narration take (or explicitly fail/retry it) instead of treating individually clean short takes as a natural 30s delivery.
- The rejected Talking asset `ca38ee20-5f81-491f-bfe4-30b191422ba2` now carries durable `human_review_state=rejected` evidence (`human-review:2026-09-14-full-flow`) with the specific lip-movement and expression findings. `TalkingHumanReview` preserves that U-Talking decision without overwriting the independent automated evidence; both VideoSpec assembly and Remotion rendering reject an AI_VIDEO with this state. Focused regression coverage proves the rejection cannot be bypassed by a later render. The first truly single-pass OmniVoice full-copy attempt, `gate-d-continuous-01.wav`, independently passes strict QA (29,080ms, 100% coverage, zero missing/duplicates, 400ms leading and 840ms longest silence). It removes the stitch mechanism but is 920ms below the R1 30s lower bound and took about 29 minutes on the CPU-only runtime, so it is an offline continuity candidate only—not a compliant 30–60s master nor evidence that the failed MuseTalk output is acceptable.
- Alternative local Talking discovery is complete for this hardware. LatentSync’s current official README specifies a minimum 8GB VRAM for 1.5 inference and 18GB for 1.6, while the available RTX 3060 Laptop has 6GB, so it is not installed or represented as runnable. Wav2Lip was isolated under `content-os-data/wav2lip*` as a non-commercial research-only comparison candidate: its historical code ran through CUDA frame/audio preprocessing after one local librosa keyword-argument compatibility patch, but the current official Google Drive folder supplies TorchScript `Wav2Lip-SD-*` files that are incompatible with the historical `state_dict` inference script. The historical official checkpoint link recorded by the upstream repository now returns HTTP 404. No unknown mirror or model conversion was used. Wav2Lip is therefore accurately blocked as non-reproducible on this machine, rather than being promoted as an alternative provider. Its downloaded incompatible files remain isolated evidence only.
- U1 face/identity consent is now recorded as `u1-20260913-biyingjie-tim-internal`: 毕英杰’s Chinese and English reference media, and TIM’s reference media, may be used for voice cloning and new local Talking/lip-sync generation **within this project only**. This supersedes the former Gate C consent blocker for these named creators; it does not authorize publication, external account sharing, a paid provider, or use outside this project.
- The candidate Chatterbox multilingual checkpoint is installed only for comparison: isolated `chatterbox-tts 0.1.7` with CPU `torch 2.6.0`, `torchaudio 2.6.0`, `transformers 5.2.0`, `diffusers 0.29.0`, and the official Perth watermark path. Its venv is 2,167,273,493 bytes (about 2.167 GB) and its six-file local checkpoint is 3,208,951,748 bytes (about 3.209 GB); it is not a Core dependency. Raw ref2 generated 5,880ms with 100% copy coverage and no leading gap but failed the 2,720ms trailing-silence check; measured trimming to 3,160ms passed. Raw ref4 generated 4,740ms; strict QA flagged five Traditional/Simplified character-code differences rather than missing spoken words, and a diagnostic script-equivalent target plus measured trim passed (4,000ms, 100% coverage, 140ms longest gap). U-Voice judged Chatterbox clearly worse in timbre similarity and naturalness, so it is rejected for the R1 default path; OmniVoice is the preferred local technical-benchmark path, still subject to its non-commercial weight/license boundary.

## Active work package

### C — Talking Provider Spike + QA

Goal:

> Produce one authorized new creator Talking/lip-sync segment through a replaceable provider boundary, with new words, basic sync/playability evidence, and no change to Core contracts.

Gate B outcome:

1. the provider-neutral Voice job, provenance and QA gates are implemented and unit-verified;
2. real authorized samples demonstrate both passing and rejected semantic/edge outcomes, so fixture success is not being promoted to product quality;
3. OmniVoice is retained only as a non-commercial offline technical benchmark; Chatterbox is measured and rejected; and
4. a commercial-safe runtime path remains unselected and must stay schema-compatible.

Required implementation order:

1. identify a suitable authorized reference Talking clip from the authorized material and record the face/identity consent subject;
2. the provider-neutral Talking generation/job path is implemented using the existing `talking` budget/idempotency and `TalkingProfile` records;
3. evaluate one real local lip-sync path without making it a base dependency or silently using an avatar/fallback;
4. verify newly spoken words, playable output, basic audio/video sync and provider/reference provenance; and
5. U-Talking accepted the real local sample as a technical candidate; retain its baked-subtitle source as evaluation-only and do not call the product gate passed until the full 30–60s assembly is produced.

## Next ready task

**Keep paid Talking providers optional and deferred. In an isolated VideoReTalking runtime, use an authorized ordinary day-to-day creator clip and the same verified driving audio to establish actual compatibility, license/runtime feasibility and new-word audio/container evidence. Then require U-Talking review of visible sync, likeness, naturalness, gaze/expression and editorial fit. Do not require special silent/closed-mouth capture, repeat the rejected MuseTalk output, use a generic avatar, or let a simple crop substitute for performance control. A candidate that fails any ordinary-material product gate is not admitted. Independently retain the 29.08s single-pass OmniVoice evidence but do not use it as a 30–60s master; any further attempt must prove a compliant duration first.**

Default implementation tier: **Terra**, because this crosses provider, job, media asset, runtime readiness and QA boundaries. Luna may handle isolated UI/tests/docs after the interfaces are fixed. Sol is not needed unless a real architecture/security conflict appears.

## Blocking human inputs

Pause only if the next real generation requires information not already explicitly authorized:

- approve first paid cloud/provider use or a budget increase.

Do not infer voice/face-cloning consent merely from general production-media permission.

## Provider/license boundary

- OmniVoice code: Apache-2.0.
- Official OmniVoice pretrained weights: currently CC-BY-NC; use only for non-commercial evaluation/benchmark unless a separate valid commercial path is obtained.
- Do not bundle those weights into commercial releases.
- Commercial-safe provider claims require source **and model-weight/dependency** license review.

## Model-cost routing

`Luna → Terra → Sol`

- Luna: isolated UI/CRUD/tests/docs/simple adapters.
- Terra: current Voice/Talking/provider/media/job work.
- Sol: architecture/security/critical quality gate or unresolved Terra reproduction only.

## Handoff checklist

Before ending an implementation package:

```text
1. Run relevant tests/builds.
2. Run: python scripts/check_docs.py
3. Update this STATUS.md with facts, active package and next ready task.
4. Update DECISIONS.md only if a durable choice changed.
5. Do not create another top-level review/freeze/handoff document.
```

For historical detail, use Git history. Current execution rules are in `CONTENT_OS_EXECUTION_SPEC.md`.
